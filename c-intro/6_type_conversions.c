/* Can be able to use the type connversion to either conduct an 
implicit - exmaple the division of the integers provides a decimal output implicitly
an explicit convesion of some of the data types

Contstats may be used in the cases where the repetition and the changes in certain variables is
not desired in most of this cases, the variables are mostly declared using an upper case as a
best practice.
*/

#include <stdio.h>


int main(){
    int x = 5;
    int y = 2;

    const int CONSTANTNAME = 2;
    
    float ans = (float) 5 / 2;

    printf("%.2f\n", ans);

    printf("%d\n", CONSTANTNAME);
}